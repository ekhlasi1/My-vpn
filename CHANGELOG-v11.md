# تغییرات نسخه v11 (پرچم کشور در نام کانفیگ‌ها)

## چه چیزی اضافه شد؟

1. ستون اختیاری `country` به جدول `backend_pool` اضافه شد (`migration-v11.sql`) — دقیقاً همان الگوی
   ستون `country` که در v10 به `clean_ips` اضافه شده بود. از تب «🖧 پنل‌ها / اکانت‌های کلادفلر»، هنگام
   افزودن یک اکانت/ورکر می‌توانید کشور آن را مشخص کنید (فقط نمایشی، تأثیری روی مسیریابی ندارد).
2. دو placeholder جدید برای «نام کانفیگ‌های پرو و تست»: `{flag}` و `{country}`. مقدار پیش‌فرض قالب پرو
   به `{flag} {brand} VIP |{country}{n} | @{admin}` تغییر کرد — برای یک کانفیگ که به اکانتی با کشور
   `AE` وصل است و برند `BNDMAX` است، دقیقاً همین را تولید می‌کند:

   ```
   🇦🇪 BNDMAX VIP |امارات1 | @vahidekhlasi
   ```

3. اگر اکانت مقصدِ یک کانفیگ کشوری تنظیم‌نشده باشد، `{flag}` و `{country}` هر دو خالی رندر می‌شوند —
   یعنی بدون هیچ کاری از سمت شما، ظاهر قبلی («سرور N») تقریباً حفظ می‌شود (فقط emoji تاج 👑 پیش‌فرض حذف
   شده؛ اگر می‌خواهید همان قالب قبلی دقیقاً برگردد، آن را در فرم «نام کانفیگ‌ها» دستی وارد کنید).

## نکته‌ی مهم — همان قانون v10

این پرچم/نام کشور کاملاً **دستی و نمایشی** است. کلادفلر ورکرز هیچ مفهومی از «کشور خروجی» ندارد و این
پروژه هیچ چیزی را در این مورد تست یا تأیید نمی‌کند (به همین دلیل بود که قابلیت قدیمی‌تر «کشورهای ساختگی»
در migration-v3.sql حذف شد). فقط برای اکانتی کشور تنظیم کنید که واقعاً می‌دانید متعلق به آن کشور است
(مثلاً ورکری که خودتان برای کاربری در آن کشور دیپلوی کرده‌اید) — در غیر این صورت به کاربرانتان چیزی
نادرست نشان می‌دهید.

## بازبینی امنیتی کوچک (بخش sync چند اکانتی)

بررسی کد `src/services/worker-sync.ts` / `src/core/handler.ts` (تب پنل‌ها، اتصال ورکر با رمز) انجام شد؛
معماری موجود (رمز مشترک بین دو ورکر، بک‌فیل خودکار کاربران هنگام افزودن اکانت جدید، health-check قبل از
قرار دادن یک اکانت در چرخش) از نظر منطقی درست است. یک اصلاح واقعی انجام شد: مقایسه‌ی رمز اتصال
(`X-Worker-Sync-Secret`) از `===` ساده به یک تابع constant-time (`timingSafeEqual` در
`src/auth/password.ts`) تغییر کرد، تا مثل هش پسورد ادمین در برابر timing attack مقاوم باشد.

## فایل‌های تغییریافته

- `src/db/schema.sql` — ستون `country` به `backend_pool`؛ مقدار پیش‌فرض `pro_config_name`
- `src/db/migration-v11.sql` — جدید (ALTER TABLE)
- `src/db/queries.ts` — `PoolWorkerRow.country`، `PoolWorkerInput.country`، `addPoolWorker(...)`،
  `getPoolRotation()` حالا `activeHosts` را با کشور هر اکانت برمی‌گرداند
- `src/utils/countries.ts` — جدید: نقشه‌ی پرچم/نام فارسی مشترک برای سمت سرور
- `src/services/subscription.ts` — placeholderهای `{flag}`/`{country}`، `renderConfigName`
- `src/api/admin.ts` — پذیرفتن `country` در `POST /pool` و `PATCH /pool/:id`
- `src/pages/admin.ts` — انتخاب‌گر کشور در فرم افزودن اکانت، ستون کشور در جدول پنل‌ها، به‌روزرسانی راهنمای
  فرم «نام کانفیگ‌ها»
- `src/auth/password.ts` — `timingSafeEqual` (تابع کمکی جدید)
- `src/core/handler.ts` — استفاده از `timingSafeEqual` برای بررسی `X-Worker-Sync-Secret`

## اجرای مایگریشن روی دیتابیس موجود

```
npx wrangler d1 execute <db-name> --remote --file=./src/db/migration-v11.sql
```

(روی یک دیپلوی کاملاً تازه لازم نیست — `schema.sql` از همان ابتدا ستون را دارد.)
