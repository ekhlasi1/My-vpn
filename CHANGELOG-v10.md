# تغییرات نسخه v10 (برچسب کشور + فایل سریع برای سرورهای اجاره‌ای)

## چه چیزی اضافه شد؟

هنوز **هیچ اسکن یا تست خودکاری وجود ندارد** (طبق تصمیم v9). دو تغییر کوچک و کاملاً دستی اضافه شد:

1. یک ستون اختیاری `country` به جدول `clean_ips` اضافه شد (`migration-v10.sql`) تا در فرم و جدول تب
   «پنل‌ها»، هر آی‌پی/پورت بتواند یک برچسب کشور (مثلاً DE، TR، AE) داشته باشد — کاملاً نمایشی، تأثیری روی
   منطق انتخاب آی‌پی ندارد.
2. فایل جدید `src/data/rented-clean-ips.ts`: برای سروری که خودتان اجاره/خریداری می‌کنید و آدرسش مرتب عوض
   می‌شود (مثلاً اجاره‌ی هفتگی)، به‌جای وارد کردن دستی هر بار از داشبورد، کافی‌ست همین فایل را ویرایش و
   دوباره دیپلوی کنید. مقادیر این فایل با لیست دیتابیس ترکیب می‌شوند (هم در نمایش داشبورد، هم در
   `getBestCleanIp`). ردیف‌های این فایل در جدول داشبورد با برچسب «📄 فایل» مشخص می‌شوند و دکمه‌ی حذف
   ندارند (چون در دیتابیس نیستند) — تغییرشان فقط از طریق ویرایش فایل + دیپلوی مجدد ممکن است.

## نکته‌ی مهم درباره‌ی این فایل

`rented-clean-ips.ts` فقط برای سرورهایی است که **خودتان مالک/مستأجر قانونی‌شان هستید** (مثلاً یک VPS با
فاکتور/پنل کنترل معتبر). این پروژه پشتیبانی یا مکانیزمی برای اسکن/استفاده از لیست‌های پروکسی عمومی یا
اسکرپ‌شده‌ی متعلق به اشخاص ثالث ندارد و اضافه نخواهد شد.

## فایل‌های تغییریافته

- `src/db/schema.sql` — ستون `country` به `clean_ips`
- `src/db/migration-v10.sql` — جدید (ALTER TABLE)
- `src/db/queries.ts` — `CleanIpRow.country`، `addCleanIp(..., country)`، ادغام با `RENTED_CLEAN_IPS`
- `src/data/rented-clean-ips.ts` — جدید
- `src/api/admin.ts` — پذیرفتن `country` در `POST /clean-ips`، رد کردن حذف ردیف‌های `file:*`
- `src/pages/admin.ts` — انتخاب‌گر کشور در فرم، ستون کشور/منبع در جدول

## اجرای مایگریشن روی دیتابیس موجود

```
npx wrangler d1 execute <db-name> --remote --file=./src/db/migration-v10.sql
```

(روی یک دیپلوی کاملاً تازه لازم نیست — `schema.sql` از همان ابتدا ستون را دارد.)
