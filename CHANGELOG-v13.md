# تغییرات نسخه v13 (Radar — کشف خودکار آی‌پی تمیز)

## چرا این نسخه؟

اسکنرهای موجود («🔍 اسکنر خودکار سلامت» و «🔎 کشف آی‌پی تمیز جدید») همیشه به یک لیست اولیه نیاز
داشتند: یا آی‌پی‌هایی که خودتان قبلاً به لیست اضافه کرده بودید، یا یک لیست کاندید که باید از
جایی (کانال تلگرام، فروم و…) پیدا و کپی می‌کردید. یعنی اگر هیچ لیستی در دست نداشتید، اسکنر کاری
برایتان نمی‌کرد. این نسخه این نیاز را حذف می‌کند.

## چه چیزی اضافه شد؟

**کارت جدید «🛰️ Radar — کشف خودکار آی‌پی تمیز (بدون نیاز به لیست)»** (بالای تب «🌐 آی‌پی‌های سالم»،
قبل از کارت دستی):

- با یک کلیک روی «🛰️ اجرای Radar روی شبکه فعلی»، حدود ۴۵ آی‌پی به‌صورت تصادفی از **رنج‌های رسمی
  Cloudflare** (همان‌هایی که در `https://www.cloudflare.com/ips-v4/` منتشر شده) ساخته می‌شود.
- این آی‌پی‌ها مستقیماً از **همان مرورگری که دکمه را زده‌اید** (نه از سرور) تست می‌شوند — یعنی
  نتیجه دقیقاً بازتاب‌دهنده‌ی وضعیت همان شبکه/اپراتوری است که الان با آن به پنل وصل شده‌اید. برای
  بهترین نتیجه، Radar را از روی گوشی/مودمی بزنید که کاربرانتان معمولاً با همان اپراتور وصل می‌شوند.
- سریع‌ترین ۱۶ مورد به‌عنوان کاندید نهایی انتخاب و خودکار به همان مکانیزم موجود «کشف آی‌پی تمیز»
  (هندشیک واقعی TLS با SNI همین دامنه، روی خود ورکر) فرستاده می‌شوند — نتیجه در همان جدول قبلی با
  دکمه‌های «➕ افزودن» نمایش داده می‌شود.
- **محدودیت شناخته‌شده**: پیش‌فیلتر داخل مرورگر (مرحله‌ی اول) به‌خاطر محدودیت گواهی TLS در
  مرورگرها، صددرصد دقیق نیست — به همین دلیل همیشه با یک هندشیک واقعی روی خود ورکر (مرحله‌ی دوم،
  دقیقاً مثل اسکنر قبلی) دوباره تأیید می‌شود. بهتر است هر چند روز یک‌بار Radar دوباره اجرا شود، چون
  کیفیت آی‌پی‌های Cloudflare روی هر اپراتور می‌تواند در طول زمان تغییر کند.
- کارت قدیمی برای وارد کردن دستی لیست/رنج کاندید همچنان کنار Radar باقی مانده (اکنون با عنوان
  «کشف آی‌پی تمیز جدید (دستی)») — برای مواقعی که یک لیست مشخص از جای دیگری دارید.

هیچ تغییری در دیتابیس یا API لازم نبود؛ Radar فقط یک لایه‌ی تولید-کاندید در سمت مرورگر روی endpoint
موجود `/api/admin/clean-ips/scan-candidates` است.
