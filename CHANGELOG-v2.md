# تغییرات این بروزرسانی

## چی اضافه شد

1. **نام کانفیگ‌ها = هویت کاربر**: هر کانفیگ VLESS حالا با نام/آیدی تلگرام کاربر و راهنمای خرید (`@<telegram_admin_username>`) نام‌گذاری می‌شود. هر کسی وارد ربات شود، در جدول جدید «لیست ورودی‌های ربات» در پنل ادمین ثبت می‌شود — حتی اگر اشتراکی نگیرد.
2. **اشتراک VIP چندلوکیشنی**: کاربران `pro` فعال، به‌جای یک لینک، چند کانفیگ می‌گیرند: یک کانفیگ پایه به‌نام خودشان + یک کانفیگ برای هر لوکیشن ویژه (مثلاً `🇩🇪 VIP BNDMAX | Germany`). همه از یک سرور/edge رد می‌شوند؛ این‌ها برچسب نمایشی برای متمایزکردن کاربران VIP هستند، نه مسیر خروجی فیزیکی جدا.
3. **اعلان مصرف هر ۴۰۰ مگابایت**: به ازای هر بار عبور از یک آستانه مصرف (پیش‌فرض ۴۰۰ مگابایت، قابل تغییر در پنل → تنظیمات تست) ربات به کاربر پیام می‌دهد.
4. **دکمه‌ها و منوی حرفه‌ای ربات**: کیبورد دائمی با دکمه‌های 🎁 دریافت تست / 📊 وضعیت مصرف / 🎖️ خرید VIP / 🌍 لوکیشن‌های VIP / ℹ️ راهنما، به‌علاوه ثبت منوی ☰ رسمی تلگرام (`setMyCommands`) هنگام فعال‌سازی Webhook.
5. **۴ لوکیشن ویژه پیش‌فرض**: آلمان 🇩🇪، امارات 🇦🇪، آمریکا 🇺🇸، ترکیه 🇹🇷 — فقط داخل ساب کاربران VIP فعال نمایش داده می‌شوند. از پنل ادمین → تب «لوکیشن‌های VIP» به‌صورت JSON قابل ویرایش/افزودن/حذف است.
6. **صفحه اصلی غیرفعال شد**: `/` حالا مستقیم پنل ادمین (ورود/داشبورد) را نشان می‌دهد. کد صفحه‌ی عمومی قبلی (`indexPage`) در `src/pages/index.ts` حذف نشده و همچنان روی مسیر `/landing` در دسترس است — برای برگرداندنش به `/`، در `src/core/handler.ts` بخش «Root» را ویرایش کنید.

## برای فورک کردن / ساخت نسخه شخصی

- یوزرنیم ادمین و نام برند از پنل ادمین (تب «ربات تلگرام») قابل تغییرند و در همه‌ی متن‌های جدید به‌جای مقدار ثابت استفاده می‌شوند.
- برای دیتابیس‌های از قبل دیپلوی‌شده (قبل از این بروزرسانی)، یک‌بار این را اجرا کنید:
  ```
  wrangler d1 execute <db-name> --remote --file=src/db/migration-v2.sql
  ```
  دیپلوی تازه نیازی به این فایل ندارد؛ `src/db/schema.sql` همه‌چیز را دارد.
