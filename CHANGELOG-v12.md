# تغییرات نسخه v12 (اسکنر کشف آی‌پی تمیز + حذف ابزار پایتون)

## چرا این نسخه؟

اسکنر قبلی («🔍 اسکنر خودکار سلامت») فقط ردیف‌هایی را تست می‌کرد که از قبل خودتان دستی به لیست
«آی‌پی‌های سالم» اضافه کرده بودید — یعنی اگر آن لیست خالی بود، دکمه‌ی اسکن عملاً کاری برای انجام
دادن نداشت و نتیجه‌ای برنمی‌گرداند (به همین دلیل «اصلاً کار نمی‌کرد»). ابزار جدا‌گانه‌ی پایتون
(`clean-ip-scanner/server.py` + لیست‌های `proxy/*.txt`) هم چون خارج از پنل ادمین و روی سیستم شخصی
اجرا می‌شد و به پروکسی‌های عمومی/ناشناس وابسته بود، حذف شد — طبق درخواست، همه‌چیز الان فقط تحت وب
و داخل پنل ادمین است.

## چه چیزی اضافه شد؟

1. **کارت جدید «🔎 کشف آی‌پی تمیز جدید»** (زیر همان تب «🌐 آی‌پی‌های سالم»):
   - یک کادر متنی که می‌توانید لیست آی‌پی‌های کاندید (مثلاً از یک کانال/لیست عمومی) را در آن
     بچسبانید — هر خط یکی، با فرمت‌های `1.2.3.4` یا `1.2.3.4:443` یا `1.2.3.4,443,DE`.
   - انتخاب «کشور پیش‌فرض» برای خط‌هایی که کشور ننوشته‌اند، و چک‌باکس/فیلد پورت سفارشی برای
     پورت‌هایی که می‌خواهید تست شوند (۴۴۳، ۲۰۵۳، ۲۰۸۳، ۲۰۸۷، ۲۰۹۶، ۸۴۴۳ یا هر پورت دلخواه).
   - **پشتیبانی از رنج CIDR**: به‌جای وارد کردن تک‌تک آی‌پی، می‌توانید یک رنج مثل `1.2.3.0/24` بدهید —
     تا سقف `/24` (۲۵۴ آی‌پی) به‌طور خودکار باز و تک‌تک تست می‌شود. رنج‌های بزرگ‌تر (`/16`, `/20` و...)
     عمداً رد می‌شوند تا ورکر روی تعداد بیش‌ازحد اتصال timeout نخورد — رنج بزرگ را به چند تکه‌ی
     `/24` تقسیم کنید.
   - دکمه‌ی «🚀 شروع اسکن» که یک هندشیک واقعی TLS با SNI همین دامنه به هر ترکیب آی‌پی:پورت می‌زند
     (دقیقاً همان مکانیزم اسکنر قبلی)، با نوار پیشرفت و جدول نتایج (کشور/آی‌پی/پورت/وضعیت/پینگ/خطا).
   - هر ردیف سالم یک دکمه‌ی «➕ افزودن» جداگانه دارد، و یک دکمه‌ی «➕ افزودن همه‌ی موارد سالم» هم
     برای افزودن یکجای همه‌ی نتایج سالم به لیست دائمی.
   - مثل اسکنر قبلی، **هیچ آدرسی از جای دیگری گرفته نمی‌شود** — فقط همان چیزی تست می‌شود که خودتان
     در کادر وارد کرده‌اید؛ سقف ۴۰۰ ترکیب آی‌پی×پورت در هر اسکن (برای جلوگیری از timeout روی ورکر).

2. **پایداری پنل ادمین**: بارگذاری اولیه‌ی تب‌ها (کاربران، پول اکانت‌ها، آی‌پی‌های سالم و...) حالا
   جدا-جدا با مدیریت خطا انجام می‌شود؛ اگر یکی از بخش‌ها به هر دلیلی خطا بدهد (مثلاً پاسخ غیرمنتظره
   از API)، فقط همان بخش پیام خطا نشان می‌دهد و بقیه‌ی پنل مثل قبل کار می‌کند، به‌جای اینکه کل صفحه
   خراب/سفید شود.

3. **حذف ابزار پایتون**: پوشه‌ی `clean-ip-scanner/` (شامل `server.py` و لیست‌های `proxy/*.txt` و
   `ips.txt`) از پروژه حذف شد. همه‌چیز الان فقط از داخل پنل ادمین (تحت وب) قابل انجام است.

4. کشور آذربایجان (🇦🇿 AZ) هم به لیست کشورهای قابل‌انتخاب اضافه شد.

## نکته‌ی مهم درباره‌ی «کشف آی‌پی تمیز از فلان کشور»

این ابزار **تولیدکننده‌ی خودکار لیست آی‌پی نیست** — Cloudflare یک شبکه‌ی anycast است و یک IP ثابت
و همیشگی برای «کشور آلمان» یا «کشور ترکیه» وجود ندارد که بشود از قبل حدس زد. کاری که این کارت انجام
می‌دهد این است: شما یک لیست کاندید (از هر منبعی که به آن اعتماد دارید) می‌دهید، و ابزار با یک
هندشیک واقعی TLS مشخص می‌کند کدام‌ها همین الان واقعاً به ورکر شما می‌رسند و پینگ‌شان چقدر است —
دقیقاً همان کاری که قبلاً با اسکنر پایتون و توکن کلادفلر روی سیستم شخصی انجام می‌شد، منتها الان
سریع‌تر، بدون نیاز به نصب چیزی، و مستقیماً از داخل خود پنل.
