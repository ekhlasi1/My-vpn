# تغییرات نسخه v9 (حذف اسکن خودکار آی‌پی، جایگزینی با لیست دستی)

## چرا این تغییر؟

بخش «آی‌پی‌های سالم خودکار» که در v6 اضافه شده بود، آی‌پی‌های کاندید کلادفلر را مستقیماً از داخل خود Worker
تست می‌کرد. این تست فقط اتصال «کلادفلر به کلادفلر» را می‌سنجید و هیچ راهی برای فهمیدن اینکه آیا آن آی‌پی از
داخل شبکه‌ی کاربر واقعی هم باز است یا نه نداشت — نتیجه‌اش دقیقاً همان مشکلی بود که در کد قبلی هم مستند شده بود:
«نشان می‌دهد سالم است ولی برای کاربر واقعی کار نمی‌کند». به همین دلیل این قابلیت به‌طور کامل حذف شد.

## چه چیزی جایگزین شد؟

هیچ تست یا اسکن خودکاری وجود ندارد. در تب «پنل‌ها»ی داشبورد، بخش «🌐 آی‌پی‌های سالم (دستی)» یک فرم ساده دارد
که فقط **آی‌پی + پورت** (و یادداشت اختیاری) می‌گیرد. وقتی گزینه‌ی «استفاده از لیست آی‌پی سالم به‌جای دامنه در
کانفیگ‌ها» را فعال کنید، آدرس اتصال (`add=`) و پورت کانفیگ‌های تولیدشده به‌صورت تصادفی از بین همین لیست دستی
انتخاب می‌شود؛ `host=`/`sni=` همچنان روی دامنه‌ی واقعی ورکر باقی می‌مانند، پس مسیریابی خراب نمی‌شود. همین لیست
برای پاسخ‌های Private DNS (`/api/dns-rules`, `/dns-query`) هم استفاده می‌شود، دقیقاً مثل قبل.

## فایل‌های حذف‌شده

- `src/services/clean-ips.ts` (کل فایل حذف شد — لیست آی‌پی‌های کاندید + منطق تست)

## فایل‌های تغییریافته

- `src/db/queries.ts` — `runCleanIpDiscovery`/`saveCleanIpResults` حذف شدند؛ به‌جایش `addCleanIp`/`deleteCleanIp`
  اضافه شد و `getBestCleanIp` حالا `{ ip, port }` از لیست دستی برمی‌گرداند (بدون فیلتر «healthy»/«تازگی»).
- `src/core/handler.ts` — زمان‌بند خودکار `maybeRunCleanIpScan` (هر ۱۰ دقیقه) کاملاً حذف شد.
- `src/api/admin.ts` — `POST /api/admin/clean-ips/refresh` حذف شد؛ به‌جایش
  `POST /api/admin/clean-ips` (افزودن) و `DELETE /api/admin/clean-ips/:id` (حذف) اضافه شد.
- `src/pages/admin.ts` — جدول نتایج تست (وضعیت/تأخیر) با فرم افزودن دستی آی‌پی:پورت جایگزین شد.
- `src/services/subscription.ts` — پورت کانفیگ حالا قابل override است (`portOverride`)، همراه با آی‌پی.
- تنظیم `auto_clean_ip_enabled` به `clean_ip_override_enabled` تغییر نام داد.

## مهاجرت دیتابیس

برای دیتابیس‌های از قبل دیپلوی‌شده (به‌خصوص اگر قبلاً migration-v6.sql را اجرا کرده‌اید)، یک‌بار اجرا کنید:

```
npx wrangler d1 execute <db-name> --remote --file=./src/db/migration-v9.sql
```

دیپلوی تازه نیازی به این فایل ندارد؛ `src/db/schema.sql` جدول جدید را مستقیماً می‌سازد.
