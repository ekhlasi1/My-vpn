# تغییرات نسخه v9 (تشخیص کشور دیتاسنتر آی‌پی سالم + تأیید دستی قبل از افزودن به لیست ربات)

## مشکل قبلی

بخش «آی‌پی سالم خودکار» (v6) فقط می‌گفت یک آی‌پی از داخل ایران قابل‌اتصال هست یا نه، ولی هیچ اطلاعی از اینکه آن
آی‌پی الان به کدام دیتاسنتر کلادفلر می‌رسد نمی‌داد — نتیجه این بود که آی‌پی‌های سالم پیشنهادی عملاً همیشه آمریکا
بودند (پینگ/سرعت پایین برای ایران) و هیچ راهی هم برای انتخاب دستی و تأیید نهایی قبل از استفاده در کانفیگ‌های کاربران
وجود نداشت — هر آی‌پی «سالم» خودکار وارد چرخه می‌شد.

## تغییرات

1. **تشخیص کشور/دیتاسنتر هر آی‌پی**: علاوه بر تست سلامت از گره‌های ایرانی check-host.net، هر آی‌پی حالا مستقیماً
   (با `cf.resolveOverride`) پروب می‌شود تا کد دیتاسنتر کلادفلر (`colo`, مثل `FRA`/`IST`) که الان به آن می‌رسد
   مشخص شود، و برای دیتاسنترهای آلمان/ترکیه/امارات/گرجستان/ارمنستان/چند کشور نزدیک دیگر یک برچسب کشور خوانا هم
   نمایش داده می‌شود — ستون جدید «کشور/دیتاسنتر» در جدول داشبورد.
2. **تأیید نهایی دستی قبل از افزودن به لیست ربات**: ستون/فیلد جدید `approved` روی جدول `clean_ips`. صرفِ سالم
   بودن دیگر کافی نیست — `getBestCleanIp()` فقط از بین آی‌پی‌های *هم سالم و هم تأییدشده* انتخاب می‌کند. تأیید فقط
   از داشبورد و با تیک‌زدن ردیف‌های موردنظر و دکمه‌ی «✅ افزودن انتخاب‌شده‌ها به لیست ربات» انجام می‌شود (و قابل
   لغو با «🗑️ حذف انتخاب‌شده‌ها»).
3. تست/رفرش خودکار دوره‌ای (`runCleanIpDiscovery`) هرگز مقدار `approved` را تغییر نمی‌دهد — فقط سلامت/کشور را
   به‌روز می‌کند؛ تأیید همیشه دست خود مدیر می‌ماند.

فایل‌های تغییریافته: `src/services/clean-ips.ts`، `src/db/queries.ts`، `src/db/schema.sql`، `src/db/migration-v9.sql`
(جدید)، `src/api/admin.ts`، `src/pages/admin.ts`.

## به‌روزرسانی دیتابیس موجود

اگر پروژه از قبل دیپلوی شده، این را یک‌بار اجرا کنید:

```
npx wrangler d1 execute <db-name> --remote --file=./src/db/migration-v9.sql
```

دیتابیس‌های تازه (فایل `src/db/schema.sql`) از اول این ستون‌ها را دارند و نیازی به این مرحله ندارند.

## نکته مهم

بعد از این تغییر، اگر قبلاً `auto_clean_ip_enabled` را روشن کرده بودید و آی‌پی‌هایی از قبل «سالم» بودند، آن‌ها
به‌صورت پیش‌فرض *تأییدنشده* (`approved = 0`) شروع می‌شوند — یعنی تا وقتی از تب مربوطه آن‌ها را انتخاب و تأیید
نکنید، کانفیگ کاربران به دامنه‌ی خود ورکر برمی‌گردد (رفتار قبل از این فیچر)، نه اینکه بی‌صدا خالی بماند.
