# تغییرات نسخه v6 (رفع باگ اتصال بین اکانت‌ها + سرعت/فیلترینگ + آی‌پی سالم خودکار)

## ۱. رفع باگ اصلی: کانفیگ‌هایی که وقتی به اکانت دیگر وصل می‌شدند کار نمی‌کردند

**علت واقعی**: وقتی یک اکانت/ورکر جدید به «پنل‌ها / اکانت‌های کلادفلر» اضافه می‌شد، هیچ‌کدام از کاربران *موجود* به دیتابیس D1 آن اکانت جدید منتقل نمی‌شدند — فقط کاربرانی که *بعد از آن لحظه* ساخته/تمدید/ویرایش می‌شدند sync می‌شدند. تست سلامت (ping) فقط اتصال به ورکر + درستی رمز را چک می‌کند، نه اینکه UUID یک کاربر خاص واقعاً آن‌جا ثبت شده یا نه — برای همین تست لینک «سالم» بود، اما هندشیک VLESS واقعی کاربران قدیمی با خطای کاربر نامعتبر رد می‌شد.

**رفع شد**:
- افزودن یک اکانت جدید حالا به‌صورت خودکار همه‌ی کاربران موجود را به آن sync می‌کند.
- ویرایش رمز/توکن یک اکانت (مثلاً وقتی بعداً پر می‌شود) هم همین backfill را دوباره اجرا می‌کند.
- دکمه‌ی دستی «🔄 Sync دستی همه کاربران به همه اکانت‌ها» در تب پنل‌ها اضافه شد، برای اطمینان یا رفع دستیِ هر sync ناقص.
- یک باگ کوچک‌تر هم رفع شد: پارامتر `path=ws` در لینک‌ها فاقد `/` بود (باید `path=%2Fws` باشد)؛ در بعضی کلاینت‌های مبتنی بر Xray هندشیک WebSocket را می‌شکست.

فایل‌های تغییریافته: `src/db/queries.ts` (`addPoolWorker`, `backfillAllUsersToPoolWorker`, `resyncAllUsersToAllAccounts`)، `src/api/admin.ts`، `src/pages/admin.ts`، `src/services/subscription.ts`.

## ۲. حجم و زمان در ساب (v2rayNG و مشابه)

هدر استاندارد `Subscription-Userinfo` (`upload=…; download=…; total=…; expire=…`) به پاسخ `/[<uuid>]` اضافه شد. اکثر کلاینت‌ها (v2rayNG، Hiddify، v2Box, NekoBox) این هدر را می‌خوانند و حجم مصرفی/باقی‌مانده و تاریخ انقضا را مستقیم در لیست سرورها نشان می‌دهند.

فایل تغییریافته: `src/core/handler.ts`.

## ۳. سرعت و گیمینگ

راهنمای کامل: `docs/speed-gaming-guide-fa.md`. خلاصه: `alpn` به لینک‌ها اضافه شد؛ باقی تنظیمات (خاموش‌کردن Mux برای گیمینگ، Fragment سمت کلاینت) باید در خودِ اپ کاربر تنظیم شود — روی یک لینک vless:// قابل تحمیل نیست.

## ۴. کاهش فیلترشدن لینک/SNI/آی‌پی

آدرس اتصال (`add=`) از دامنه‌ای که در `host=`/`sni=` استفاده می‌شود جدا شد (`ipOverride` در `generateSubscription`)، تا بتوان یک آی‌پی تمیز کلادفلر را بدون خراب‌کردن مسیریابی جایگزین کرد. `alpn=h2,http/1.1` هم برای شبیه‌ترشدن به یک اتصال مرورگر واقعی اضافه شد.

## ۵. آی‌پی‌های سالم خودکار (لوکیشن ایران)

سرویس جدید `src/services/clean-ips.ts`: با استفاده از قابلیت مخصوص Cloudflare Workers به نام `cf.resolveOverride`، چند آی‌پی از رنج‌های شناخته‌شده‌ی کلادفلر مستقیماً از خود Worker متصل و تست می‌شوند (بدون تغییر SNI/Host). نتایج در جدول جدید `clean_ips` ذخیره می‌شود و بهترین آی‌پی سالم به‌طور خودکار به‌عنوان `add=` در تمام کانفیگ‌های تولیدی استفاده می‌شود.

- اجرای خودکار: همراه با نگهداری ساعتی خودِ پروژه (بدون نیاز به cron trigger اضافه).
- تب داشبورد جدید: «🌐 آی‌پی‌های سالم کلادفلر» با دکمه‌ی تست فوری.
- برای دیتابیس‌های از قبل دیپلوی‌شده، یک‌بار اجرا کنید:
  ```
  wrangler d1 execute <db-name> --remote --file=src/db/migration-v6.sql
  ```
  دیپلوی تازه نیازی به این فایل ندارد؛ `src/db/schema.sql` همه‌چیز را دارد.

فایل‌های جدید: `src/services/clean-ips.ts`، `src/db/migration-v6.sql`.
