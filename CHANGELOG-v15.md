# نسخه v15 — ایزوله‌سازی کامل اسکن IP و رفع تداخل با پنل

- هیچ اسکن IP یا Probe در زمان ورود به پنل، تغییر تب، رفرش یا باز ماندن صفحه اجرا نمی‌شود.
- Polling خودکار ۲۰ ثانیه‌ای داشبورد حذف شد تا درخواست‌های پس‌زمینه با APIهای پنل و اسکن تداخل نکنند.
- ورودی اسکن دستی دیگر در زمان بارگذاری با صدها رنج IP پر نمی‌شود؛ اسکن فقط پس از فشردن «شروع» آغاز می‌شود.
- اسکن‌های Clean-IP، اسکن کاندیدا و Radar دارای وضعیت Start/Stop مستقل شدند.
- Stop با AbortController درخواست/Probe جاری را نیز متوقف می‌کند.
- اسکن کاندیدا تا زمانی که Stop زده نشود یا همه صفحات واقعاً تمام نشوند ادامه پیدا می‌کند.
- Radar نیز تا پایان واقعی یا Stop ادامه پیدا می‌کند.
- خطای scope مربوط به handler توقف Probe اصلاح شد.
- بررسی syntax جاوااسکریپت داشبورد با `node --check` با موفقیت انجام شد.
