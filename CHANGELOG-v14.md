# نسخه v14 — رفع گیر کردن داشبورد ادمین

- آمار داشبورد دیگر همه‌ی کاربران را برای محاسبه‌ی ۶ عدد دانلود نمی‌کند؛ آمار با یک query تجمیعی D1 محاسبه می‌شود.
- ناوبری تب‌ها با event delegation بازنویسی شد تا خطای یک بخش نتواند listenerهای تب‌ها را از کار بیندازد.
- بارگذاری بخش‌ها lazy شده است؛ هنگام ورود فقط نمای کلی، سهمیه و لینک مدیر بارگذاری می‌شوند و بقیه هنگام باز شدن تب مربوطه خوانده می‌شوند.
- بنر خطا دیگر صفحه را با اطلاعات دیباگ داخلی آلوده نمی‌کند و خطاهای بارگذاری به‌صورت کنترل‌شده نمایش داده می‌شوند.
- لینک خروج و اسکرول تب‌ها نیز در برابر خطاهای DOM مقاوم‌تر شدند.
